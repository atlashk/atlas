spring:
  grpc:
    client:
      channels:
        identity-service:
          address: "${API_CLIENT_GRPC_IDENTITY_SERVICE_ADDRESS:static://127.0.0.1:50051}"
          negotiation-type: plaintext
          enable-keep-alive: true
          keep-alive-without-calls: true
