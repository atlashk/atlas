spring:
  grpc:
    client:
      channels:
        catalog-service:
          address: "${API_CLIENT_GRPC_CATALOG_SERVICE_ADDRESS:static://127.0.0.1:50052}"
          negotiation-type: plaintext
          enable-keep-alive: true
          keep-alive-without-calls: true
