grpc:
  client:
    auth-server:
      enableKeepAlive: 'true'
      negotiationType: plaintext
      keepAliveWithoutCalls: 'true'
      address: ${API_CLIENT_GRPC_AUTH_SERVER_ADDRESS:'static://127.0.0.1:50061'}
