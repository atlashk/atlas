spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${DB_URL:jdbc:postgresql://localhost:5432/db_notification}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:admin@123}
