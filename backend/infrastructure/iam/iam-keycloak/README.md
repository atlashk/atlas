# Keycloak Setup

Access to Realm `atlas`.

## Creating Realm roles

Creating two Realm roles: `admin` and `user`.

## Creating User Profile Attributes

Creating the following User Profile Attributes: `userId` and `phoneNumber` which can view and edit
by above roles.

## Creating Client Scope Mappers
