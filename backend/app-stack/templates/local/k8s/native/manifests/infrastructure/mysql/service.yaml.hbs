{{#hasService "datasource" "mysql"}}
apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: {{dotenv.PROJECT_NAME}}
  labels:
    app: mysql
spec:
  clusterIP: None
  selector:
    app: mysql
  ports:
    - name: mysql
      port: 3306
      targetPort: mysql
{{/hasService}}
