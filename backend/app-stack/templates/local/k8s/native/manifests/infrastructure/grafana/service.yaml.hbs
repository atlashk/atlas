{{#hasGrafana}}
apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: {{dotenv.PROJECT_NAME}}
  labels:
    app: grafana
spec:
  type: ClusterIP
  selector:
    app: grafana
  ports:
    - port: 3000
      targetPort: 3000
      name: http
{{/hasGrafana}}
