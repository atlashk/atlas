{{#hasService "kv-store" "redis"}}
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: {{dotenv.PROJECT_NAME}}
  labels:
    app: redis
spec:
  clusterIP: None
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
      name: redis
{{/hasService}}
