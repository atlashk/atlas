apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: {{dotenv.PROJECT_NAME}}
  labels:
    app: payment-service
    tier: backend
spec:
  selector:
    app: payment-service
  ports:
    - port: 8085
      targetPort: 8085
      name: http
  type: ClusterIP
