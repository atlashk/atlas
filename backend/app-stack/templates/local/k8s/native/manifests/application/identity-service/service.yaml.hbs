apiVersion: v1
kind: Service
metadata:
  name: identity-service
  namespace: {{dotenv.PROJECT_NAME}}
  labels:
    app: identity-service
    tier: backend
spec:
  selector:
    app: identity-service
  ports:
    - port: 8081
      targetPort: 8081
      name: http
    {{#hasService "internal" "grpc"}}
    - port: 50051
      targetPort: 50051
      name: grpc
    {{/hasService}}
  type: ClusterIP
