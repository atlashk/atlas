apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: {{dotenv.PROJECT_NAME}}
  labels:
    app: order-service
spec:
  selector:
    app: order-service
  type: ClusterIP
  ports:
    - port: 8084
      targetPort: 8084
      name: http
