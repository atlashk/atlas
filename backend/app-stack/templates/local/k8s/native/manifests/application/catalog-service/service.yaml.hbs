apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: {{dotenv.PROJECT_NAME}}
  labels:
    app: catalog-service
spec:
  selector:
    app: catalog-service
  type: ClusterIP
  ports:
    - port: 8082
      targetPort: 8082
      name: http
    {{#hasService "internal" "grpc"}}
    - port: 50052
      targetPort: 50052
      name: grpc
    {{/hasService}}
