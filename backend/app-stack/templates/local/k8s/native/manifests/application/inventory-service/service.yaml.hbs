apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: {{dotenv.PROJECT_NAME}}
  labels:
    app: inventory-service
    tier: backend
spec:
  selector:
    app: inventory-service
  ports:
    - port: 8083
      targetPort: 8083
      name: http
  type: ClusterIP
