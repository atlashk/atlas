{{#hasService "observability.logging.stack" "loki"}}
apiVersion: v1
kind: Service
metadata:
  name: loki
  namespace: {{dotenv.PROJECT_NAME}}
  labels:
    app: loki
spec:
  clusterIP: None
  selector:
    app: loki
  ports:
    - name: http
      port: 3100
      targetPort: http
{{/hasService}}
