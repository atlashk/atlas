{{#hasService "kv-store" "redis"}}
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: {{dotenv.PROJECT_NAME}}
  labels:
    app: redis
spec:
  clusterIP: None
  selector:
    app: redis
  ports:
    - name: redis
      port: 6379
      targetPort: redis
{{/hasService}}
