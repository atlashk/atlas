#!/bin/bash

# Ensures that the script exits immediately if any command fails, or if you try to use an undefined variable.
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/../../.." && pwd)"
PROJECT_NAME="atlas"

# Service definitions with name and build context
SERVICES=(
    "iam-service:$PROJECT_ROOT/backend/services/iam-service/iam-bootstrap"
    "product-service:$PROJECT_ROOT/backend/services/product-service/product-bootstrap"
    "order-service:$PROJECT_ROOT/backend/services/order-service/order-bootstrap"
    "payment-service:$PROJECT_ROOT/backend/services/payment-service/payment-bootstrap"
    "notification-service:$PROJECT_ROOT/backend/services/notification-service/notification-bootstrap"
    "api-gateway:$PROJECT_ROOT/backend/services/api-gateway/spring-cloud-gateway/spring-cloud-gateway-server"
{{#if (eq (lower stack.service-discovery) "eureka")}}
    "eureka-server:$PROJECT_ROOT/backend/platform/discovery-server/eureka-server"
{{/if}}
)

# Default values
CLUSTER_TYPE=""
CLUSTER_NAME=""

detect_cluster() {
    if command -v kubectl >/dev/null 2>&1; then
        local current_context
        current_context="$(kubectl config current-context 2>/dev/null || true)"
        case "$current_context" in
            minikube)
                if command -v minikube >/dev/null 2>&1; then
                    CLUSTER_TYPE="minikube"
                    CLUSTER_NAME="minikube"
                fi
                ;;
            kind-*)
                if command -v kind >/dev/null 2>&1; then
                    CLUSTER_TYPE="kind"
                    CLUSTER_NAME="${current_context#kind-}"
                fi
                ;;
        esac
    fi
}

load_image_into_cluster() {
    local image="$1"
    case "$CLUSTER_TYPE" in
        minikube)
            echo "Loading image $image into Minikube..."
            minikube image load "$image"
            ;;
        kind)
            echo "Loading image $image into Kind cluster \"$CLUSTER_NAME\"..."
            kind load docker-image "$image" --name "$CLUSTER_NAME"
            ;;
    esac
}

# Usage function
usage() {
    echo "Usage: $0 [OPTIONS]"
    echo "Options:"
    echo "  -h, --help              Show this help message"
    echo ""
    echo "Auto-load: If kubectl context is minikube or kind, images are loaded into cluster"
    echo "Available services:"
    for service in "${SERVICES[@]}"; do
        name="${service%%:*}"
        echo "    - $name"
    done
    echo ""
    echo "Examples:"
    echo "  $0                              # Build all Docker images"
}

# Parse named parameters
for arg in "$@"; do
    case $arg in
        -h|--help)
            usage
            exit 0
            ;;
        *)
            echo "Unknown parameter: $arg" >&2
            usage
            exit 1
            ;;
    esac
done

# Build Docker images
echo "Starting Docker image builds..."

detect_cluster
if [ -n "$CLUSTER_TYPE" ]; then
    echo "Detected Kubernetes context: $CLUSTER_TYPE ($CLUSTER_NAME). Images will be loaded into this cluster."
else
    echo "No supported Kubernetes context detected. Images will be built locally."
fi

# Validate services
if [ "${#SERVICES[@]}" -eq 0 ]; then
    echo "No services specified to build." >&2
    exit 1
fi

# Build images
for service in "${SERVICES[@]}"; do
    name="${service%%:*}"
    context="${service#*:}"

    echo "Building Docker image for $PROJECT_NAME-$name..."
    if ! docker build -t "$PROJECT_NAME-$name" "$context"; then
        echo "Failed to build Docker image for $PROJECT_NAME-$name." >&2
        exit 1
    fi
    echo "Built Docker image for $PROJECT_NAME-$name successfully."
    if [ -n "$CLUSTER_TYPE" ]; then
        load_image_into_cluster "$PROJECT_NAME-$name"
    fi
    echo
done

echo "Docker build process completed successfully."
