# Resources have been split into separate files:
#   vpc.tf  — VPC module
#   eks.tf  — EKS cluster + node group
#   irsa.tf — IAM roles for EBS CSI and Load Balancer Controller
#   helm.tf — Helm release for AWS Load Balancer Controller
