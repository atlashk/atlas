apiVersion: v2
name: common
description: Common Kubernetes resources that are shared across multiple microservices in the Atlas platform (e.g., namespaces, RBAC, etc.)
type: application
version: 1.0.0
appVersion: "1.0.0"
