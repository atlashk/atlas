{{- if eq .Values.appStack.idp "spring" }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ lower $.Release.Name }}-authorization-server
  labels:
    app.kubernetes.io/name: authorization-server
    app.kubernetes.io/instance: {{ lower $.Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version }}
spec:
  replicas: {{ .Values.global.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: authorization-server
      app.kubernetes.io/instance: {{ lower $.Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: authorization-server
        app.kubernetes.io/instance: {{ lower $.Release.Name }}
    spec:
      {{- with .Values.nodeGroups.app.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeGroups.app.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ .Values.commonReleaseName }}-{{ .Values.global.serviceAccount.name | default "namespace-reader" }}
      {{- $deps := list -}}
      {{- if eq .Values.appStack.datasource "mysql" -}}
      {{- $deps = append $deps (dict "name" "mysql" "host" (include "atlas.infra.mysql" .) "port" .Values.mysql.service.ports.mysql) -}}
      {{- end -}}
      {{- if eq .Values.appStack.datasource "postgres" -}}
      {{- $deps = append $deps (dict "name" "postgres" "host" (include "atlas.infra.postgres" .) "port" .Values.postgres.service.ports.postgres) -}}
      {{- end -}}
      {{- if eq .Values.appStack.kvStore "redis" -}}
      {{- $deps = append $deps (dict "name" "redis" "host" (include "atlas.infra.redis" .) "port" .Values.redis.service.ports.redis) -}}
      {{- end -}}
      {{- include "atlas.waitFor.initContainers" (dict "deps" $deps) | nindent 6 }}
      containers:
        - name: authorization-server
          image: "{{ if .Values.global.image.registry -}}{{ .Values.global.image.registry }}/{{ end -}}{{ .Values.authorizationServer.image.repository }}:{{ .Values.authorizationServer.image.tag | default .Values.global.image.tag }}"
          imagePullPolicy: {{ .Values.global.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.authorizationServer.service.ports.http }}
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: {{ .Values.global.springProfilesActive | quote }}
            {{- if eq .Values.appStack.datasource "mysql" }}
            - name: DB_URL
              value: "jdbc:mysql://{{ include "atlas.infra.mysql" . }}:{{ .Values.mysql.service.ports.mysql }}/db_user?useUnicode=yes&characterEncoding=UTF-8&allowPublicKeyRetrieval=true&useSSL=false"
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: {{ include "atlas.infra.mysql" . }}
                  key: user
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "atlas.infra.mysql" . }}
                  key: password
            {{- end }}
            {{- if eq .Values.appStack.datasource "postgres" }}
            - name: DB_URL
              value: "jdbc:postgresql://{{ include "atlas.infra.postgres" . }}:{{ .Values.postgres.service.ports.postgres }}/db_user"
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: {{ include "atlas.infra.postgres" . }}
                  key: user
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "atlas.infra.postgres" . }}
                  key: password
            {{- end }}
            {{- if eq .Values.appStack.kvStore "redis" }}
            - name: REDIS_HOST
              value: {{ include "atlas.infra.redis" . | quote }}
            - name: REDIS_PORT
              value: {{ .Values.redis.service.ports.redis | quote }}
            {{- end }}
            {{- if eq .Values.appStack.observability.tracing "zipkin" }}
            - name: ZIPKIN_ENDPOINT
              value: "http://{{ include "atlas.infra.zipkin" . }}:{{ .Values.zipkin.service.ports.http }}/api/v2/spans"
            {{- end }}
            {{- with .Values.authorizationServer.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          startupProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 30
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            {{ toYaml .Values.authorizationServer.resources | nindent 12 }}
          volumeMounts:
            - name: authorization-server-logs
              mountPath: /var/log/authorization-server
      volumes:
        - name: authorization-server-logs
          emptyDir: {}
      terminationGracePeriodSeconds: 30
{{- end }}
