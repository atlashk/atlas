package {{basePackage}}.repository;

import {{basePackage}}.entity.{{entity.name}};
import java.util.Optional;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

@Repository
public interface {{entity.name}}Repository extends JpaRepository<{{entity.name}}, {{entityIdJavaType entity}}> {
{{#if (eq ../stack.authentication 'jwt')}}
{{#if (eq entity.name ../jwtUserEntityName)}}
{{#if ../jwtUsernameFieldName}}
    Optional<{{entity.name}}> findBy{{capitalize ../jwtUsernameFieldName}}(String {{lowerCamel ../jwtUsernameFieldName}});
{{/if}}
{{/if}}
{{/if}}
}
