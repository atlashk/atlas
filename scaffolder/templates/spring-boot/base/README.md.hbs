# {{projectName}}

Generated by Scaffolder.

## Stack

- Spring Boot 4.0.1
{{#if (eq stack.database 'postgresql')}}- PostgreSQL{{/if}}
{{#if (eq stack.database 'mysql')}}- MySQL{{/if}}
{{#if (eq stack.authentication 'jwt')}}- JWT Authentication{{/if}}

## Getting Started

### Prerequisites

- Java 17
- Gradle
{{#if (contains stack.deployment 'docker-compose')}}- Docker + Docker Compose{{/if}}
{{#if (contains stack.deployment 'kubernetes')}}- Kubernetes + kubectl{{/if}}
{{#if (contains stack.deployment 'helm')}}- Helm{{/if}}

### Build

```bash
gradle build
```

### Run

```bash
gradle bootRun
```

{{#if (contains stack.deployment 'docker-compose')}}
### Docker Compose

```bash
docker compose up --build
```
{{/if}}

{{#if (contains stack.deployment 'kubernetes')}}
### Kubernetes

```bash
kubectl apply -f k8s/
```
{{/if}}

{{#if (contains stack.deployment 'helm')}}
### Helm

```bash
helm install {{projectName}} helm/{{projectName}}
```
{{/if}}
