apiVersion: v1
kind: Service
metadata:
  name: {{projectName}}
  labels:
    app.kubernetes.io/name: {{projectName}}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: {{projectName}}
  ports:
    - name: http
      port: 80
      targetPort: 8080
