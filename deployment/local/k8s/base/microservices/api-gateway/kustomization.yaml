apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: api-gateway
  labels:
    app: api-gateway
    tier: backend

resources:
  - pvc.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app: api-gateway
  tier: backend 
