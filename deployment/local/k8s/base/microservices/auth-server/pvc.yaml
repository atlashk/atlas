apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: auth-server-logs
  labels:
    app: auth-server
    tier: backend
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard 
