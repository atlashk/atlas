apiVersion: v1
kind: Service
metadata:
  name: auth-server
  labels:
    app: auth-server
    tier: backend
spec:
  selector:
    app: auth-server
  ports:
    - port: 8091
      targetPort: 8091
      name: http
  type: ClusterIP 
