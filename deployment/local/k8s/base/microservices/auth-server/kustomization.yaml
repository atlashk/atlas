apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: auth-server
  labels:
    app: auth-server
    tier: backend

resources:
  - pvc.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app: auth-server
  tier: backend 
