apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: product-service
  labels:
    app: product-service
    tier: backend

resources:
  - pvc.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app: product-service
  tier: backend 
