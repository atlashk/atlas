apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: order-service-logs
  labels:
    app: order-service

    tier: backend
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard 
