apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: order-service
    labels:
    app: order-service
    tier: backend

resources:
  - pvc.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app: order-service
  tier: backend 
