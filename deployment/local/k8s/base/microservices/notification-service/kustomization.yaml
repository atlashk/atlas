apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: notification-service
  labels:
    app: notification-service

    tier: backend

resources:
  - pvc.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app: notification-service

  tier: backend 
