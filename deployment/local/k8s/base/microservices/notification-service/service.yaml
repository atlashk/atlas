apiVersion: v1
kind: Service
metadata:
  name: notification-service
  labels:
    app: notification-service

    tier: backend
spec:
  selector:
    app: notification-service
  ports:
    - port: 8084
      targetPort: 8084
      name: http
  type: ClusterIP 
