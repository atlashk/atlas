apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: user-service-logs
    labels:
    app: user-service
    tier: backend
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard 
