apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: redis
  labels:
    app: redis
    tier: infrastructure

resources:
  - configmap.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app: redis
  tier: infrastructure 
