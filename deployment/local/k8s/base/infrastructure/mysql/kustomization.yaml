apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: mysql
  labels:
    app: mysql
    tier: infrastructure

resources:
  - secret.yaml
  - configmap.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app: mysql
  tier: infrastructure 
