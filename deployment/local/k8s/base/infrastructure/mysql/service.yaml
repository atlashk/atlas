apiVersion: v1
kind: Service
metadata:
  name: mysql
  labels:
    app: mysql
    tier: infrastructure
spec:
  selector:
    app: mysql
  ports:
    - port: 3306
      targetPort: 3306
      name: mysql
  type: ClusterIP 
