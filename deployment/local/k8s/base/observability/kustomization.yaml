apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: observability
  labels:
    tier: infrastructure

resources:
  - zipkin.yaml
  - prometheus.yaml
  - grafana.yaml
  - smtp4dev.yaml

commonLabels:
  tier: infrastructure 
